<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.cafeguidebook.mapper.CartMapper">
  <insert id="insertCartItem">
    INSERT INTO cart (name, price, cafe_id, menu_id, count, user_id)
    VALUES (#{cartItem.name}, #{cartItem.price}, #{cartItem.cafeId},
            #{cartItem.menuId}, #{cartItem.count}, #{cartItem.userId})
  </insert>
</mapper>